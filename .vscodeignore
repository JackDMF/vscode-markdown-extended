# Build outputs and sources
.vscode/**
.vscode-test/**
src/**
out/**
test/**
**/*.map

# Configuration and development files
.gitignore
tsconfig.json
esbuild.js
eslint.config.js
vsc-extension-quickstart.md
.idea/**
.mocharc.json
**/*.ts

# Documentation and test files
docs/**
sublime/TEST.md
sublime/TESTS.md
sublime/TEXTASTIC_INSTALL.md
sublime/TEXTASTIC_QUICK_REF.md

# Build artifacts
*.vsix
# NOTE: node_modules is NOT excluded to include markdown-it plugin dependencies
# These plugins are externalized (not bundled) in esbuild.js and loaded at runtime
# This results in ~4000 files and 12MB package size, but ensures reliable plugin loading
# TODO: Investigate bundling markdown-it plugins to reduce package size (see GitHub issue #XX)
dist/bundle-stats.html
dist/*.map

# Keep only the bundled output
!dist/extension.js
